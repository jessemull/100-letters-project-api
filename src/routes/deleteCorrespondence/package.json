{
  "dependencies": {
    "aws-sdk": "^2.1360.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "typescript": "^5.2.2"
  },
  "main": "dist/index.js",
  "name": "deleteCorrespondence",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "package": "zip -r -j dist/deleteCorrespondence.zip dist/*"
  },
  "version": "1.0.0"
}
